/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  poweredByHeader: false,
  reactStrictMode: true,
  reactCompiler: true,
};

export default nextConfig;
